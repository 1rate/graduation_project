global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']

  # Service scrape targets are added in Phase 6 once /metrics endpoints
  # are wired into ingest/stt-worker/sentiment-worker/category-worker/indexer/analytics.
